{
  "version": "1.1.1",
  "slug": "aspect-ratio",
  "section": "components",
  "parts": [
    {
      "directive": "RdxAspectRatioDirective",
      "selector": "[rdxAspectRatio]",
      "exportAs": "rdxAspectRatio",
      "description": "Maintains a fixed aspect ratio for its content using the native CSS `aspect-ratio` property.\nPlace the content inside (e.g. an `<img class=\"size-full object-cover\">`) and it fills the box.",
      "inputs": [
        {
          "name": "ratio",
          "type": "number",
          "default": "1",
          "description": "The desired aspect ratio (e.g. `16 / 9`). Defaults to `1` (a square, 1:1)."
        }
      ],
      "outputs": []
    }
  ]
}

{
  "version": "1.1.1",
  "slug": "time-field",
  "section": "components",
  "parts": [
    {
      "directive": "RdxTimeFieldInputDirective",
      "selector": "[rdxTimeFieldInput]",
      "inputs": [
        {
          "name": "part",
          "type": "SegmentPart",
          "description": "The part of the date to render\n`'hour' | 'minute' | 'second' | 'dayPeriod' | 'literal' | 'timeZoneName'`"
        }
      ],
      "outputs": []
    },
    {
      "directive": "RdxTimeFieldRootDirective",
      "selector": "[rdxTimeFieldRoot]",
      "exportAs": "rdxTimeFieldRoot",
      "inputs": [
        {
          "name": "dir",
          "type": "Direction | undefined",
          "default": "undefined"
        },
        {
          "name": "disabled",
          "type": "boolean",
          "default": "false"
        },
        {
          "name": "granularity",
          "type": "Granularity",
          "description": "The granularity to use for formatting the field. Defaults to 'day' if a CalendarDate is provided, otherwise defaults to 'minute'.\nThe field will render segments for each part of the date up to and including the specified granularity."
        },
        {
          "name": "hideTimeZone",
          "type": "boolean",
          "default": "false",
          "description": "Whether or not to hide the time zone segment of the field."
        },
        {
          "name": "hourCycle",
          "type": "HourCycle",
          "description": "The hour cycle to use for formatting times. Defaults to the locale preference"
        },
        {
          "name": "locale",
          "type": "string",
          "default": "'en'",
          "description": "The locale to use for formatting dates."
        },
        {
          "name": "maxValue",
          "type": "TimeValue",
          "description": "The maximum valid date that can be entered."
        },
        {
          "name": "minValue",
          "type": "TimeValue",
          "description": "The minimum valid date that can be entered."
        },
        {
          "name": "placeholder",
          "type": "TimeValue | undefined",
          "default": "this.defaultDate().copy()",
          "description": "The placeholder date, which is used to determine what month to display when no date is selected. This updates as the user navigates the calendar and can be used to programmatically control the calendar view"
        },
        {
          "name": "readonly",
          "type": "boolean",
          "default": "false",
          "description": "Whether or not the field is readonly."
        },
        {
          "name": "step",
          "type": "DateStep",
          "description": "The stepping interval for the time fields. Defaults to 1"
        },
        {
          "name": "value",
          "type": "TimeValue | null",
          "default": "null",
          "description": "The controlled time value. `null` represents an empty field and keeps the value addressable as\nan Angular Signal Forms child field (`undefined` denotes an absent optional path)."
        },
        {
          "name": "dirty",
          "type": "boolean",
          "default": "false",
          "description": "Whether the value changed from its initial value. Merged with internal tracking."
        },
        {
          "name": "errors",
          "type": "readonly RdxValidationError[]",
          "default": "[]",
          "description": "Validation errors for the control. A non-empty list marks it invalid."
        },
        {
          "name": "invalid",
          "type": "boolean",
          "default": "false",
          "description": "Whether the control is invalid. A non-empty {@link errors} list also marks it invalid."
        },
        {
          "name": "pending",
          "type": "boolean",
          "default": "false",
          "description": "Whether async validation is pending. Pending controls publish neither `data-valid` nor `data-invalid`."
        },
        {
          "name": "touched",
          "type": "boolean",
          "default": "false",
          "description": "Whether the control has been touched. A `model()` so Signal Forms can write it; set on blur/focus-out."
        }
      ],
      "outputs": [
        {
          "name": "touch",
          "type": "void",
          "description": "Emits when the control is touched, notifying Signal Forms (stable Angular 22 contract)."
        }
      ],
      "twoWay": [
        "placeholder",
        "value",
        "touched"
      ]
    }
  ]
}

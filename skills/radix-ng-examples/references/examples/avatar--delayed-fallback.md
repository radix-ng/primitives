# Avatar — Delayed fallback

> One example from the [Avatar](../components/avatar.md) index — imports, anatomy, and links to the API and styling contracts are there.

> Generated from `@radix-ng/primitives@1.1.1` — if the installed version differs, verify the API against the installed package.

`delayMs` waits before showing the fallback, so it only appears for slower connections.

```html
<rdx-avatar-delay />
```

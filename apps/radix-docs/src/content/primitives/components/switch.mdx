---
title: Switch
slug: switch
section: components
description: .
---

# Switch

<Description>A control that allows the user to toggle between checked and not checked.</Description>

<ComponentPreview name="switch" file="switch-demo" />

<FeatureList items={[
    'Can be controlled or uncontrolled.',
    'Full keyboard navigation.'
]} />

## Anatomy

```html
<button rdxSwitchRoot>
    <input rdxSwitchInput />
    <span rdxSwitchThumb></span>
</button>
```

## API Reference

### Root
`RdxSwitchRoot`

Contains all the parts of a switch. An optional `input` renders when used within a `form` to submit a value.
<PropsTable name="RdxSwitchRoot" />

<EmitsTable name="RdxSwitchRoot" />

<DataAttributesTable attributes={[
    { name: '[data-checked]', value: 'Present when the switch is on.' },
    { name: '[data-unchecked]', value: 'Present when the switch is off.' },
    { name: '[data-disabled]', value: 'Present when disabled.' },
    { name: '[data-readonly]', value: 'Present when read-only.' },
    { name: '[data-required]', value: 'Present when required.' }
]} />

### Input
`RdxSwitchInput`

The hidden native checkbox used for form submission and screen readers.

### Thumb
`RdxSwitchThumb`

The thumb that is used to visually indicate whether the switch is on or off.
<DataAttributesTable attributes={[
    { name: '[data-checked]', value: 'Present when the switch is on.' },
    { name: '[data-unchecked]', value: 'Present when the switch is off.' },
    { name: '[data-disabled]', value: 'Present when disabled.' }
]} />

## Accessibility
Adheres to the [`Switch` role requirements](https://www.w3.org/WAI/ARIA/apg/patterns/switch).

### Keyboard Interactions

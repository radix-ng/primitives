---
import { cn } from '@/utils/utils';

const { className, ...props } = Astro.props;
---

<h2 class={cn('mt-12 -scroll-m-36 text-[27px] font-semibold tracking-[-0.01em] first:mt-0', className)} {...props}>
    <slot />
</h2>

<script lang="ts">
    import MainLayout from './components/main-layout.analog' with { analog: 'imports' };

    import { RouterOutlet } from '@angular/router';

    defineMetadata({ imports: [RouterOutlet] });

</script>

<template>
    <tx-main-layout />
</template>

<script lang="ts">
    import MainLayout from './components/main-layout.analog' with { analog: 'imports' };

    import { RouterOutlet } from '@angular/router';

    defineMetadata({ selector: 'tx-app', imports: [RouterOutlet] });

</script>

<template>
    <tx-main-layout></tx-main-layout>
</template>
